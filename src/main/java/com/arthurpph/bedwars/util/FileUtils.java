package com.arthurpph.bedwars.util;

public final class FileUtils {
//    public static void copyFolder(File source, File target) throws IOException {
//        Path sourcePath = source.toPath();
//        Path targetPath = target.toPath();
//
//        Files.walk(sourcePath).forEach(path -> {
//            try {
//                Path destination = targetPath.resolve(sourcePath.relativize(path));
//                Files.copy(path, destination, StandardCopyOption.REPLACE_EXISTING);
//            } catch (IOException e) {
//                e.printStackTrace();
//            }
//        });
//    }
//    TODO: use subfolder to find game world for better organization
}
